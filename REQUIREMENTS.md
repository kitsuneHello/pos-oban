# **システム仕様書：1団体専用POSレジWebアプリケーション (for OpenCode)**

## **1\. プロジェクト概要**

本システムは、学校の文化祭や学園祭において**1つの団体（クラス・部活・有志グループ等）が専用で運用**することを前提とした、レスポンシブWebアプリケーションです。

### **1.1 コア前提条件**

1. **単一団体専用**: 複数店舗・本部統括機能や複雑なユーザー管理を排除。  
2. **ユーザー名＋共通パスワード認証**: ユーザー名は `users.txt` に1行ずつ追記して管理（追記直後から有効、再起動不要）。パスワードは全ユーザー共通（デフォルト: 1234）。  
3. **現金決済専用**: 決済方法は「現金のみ」。お釣り計算と現金売上管理に特化。  
4. **店頭対面オーダー**: モバイルオーダーではなく、レジ窓口での対面受付。  
5. **全トッピング自由選択（重要）**:  
   * メイン商品とトッピングの事前紐づけ（個別グループ設定）は行わない。  
   * メイン商品選択後、**マスター登録されている全種類のトッピングの中から任意の個数（0, 1, 2...）と組み合わせを自由選択**可能。  
   * 各トッピングは個別に「在庫数」および「SOLD OUT（品切れ）」管理を行う。  
6. **マルチデバイス・リアルタイム同期**: 複数端末（レジ、キッチン、呼び出し、在庫、ダッシュボード）間でBroadcastChannel / LocalStorage（またはWebSocket/DB通信）を介して状態をリアルタイム同期。

## **2\. システム構成・画面モジュール仕様**

アプリは共通ヘッダー（タブ切り替え付き）を持ち、以下の5つの機能モジュールで構成されます。

### **2.1 共通機能・認証**

* **ログイン（モーダル/オーバーレイ）**: ユーザー名と共通パスワードを入力して認証。ユーザー名は `users.txt` に1行ずつ追記して追加（`#` 行はコメント）。セッション（SessionStorage）で保持。  
* **ヘッダー表示**: 団体名、オンライン同期ステータス、画面切り替えタブ（レジ / キッチン / 呼び出し / 在庫 / ダッシュボード）、ログアウトボタン。

### **2.2 レジ会計モジュール (POS Terminal)**

* **メイン商品一覧（グリッド表示）**:  
  * 商品名、基本価格、リアルタイム在庫数、SOLD OUT状態を表示。  
  * 商品タップで「トッピングカスタマイズモーダル」を起動。  
* **トッピングカスタマイズモーダル（仕様更新）**:  
  * 選択したメイン商品名と基本価格を表示。  
  * マスター登録されている**全トッピング**を一覧表示。  
  * 各トッピングごとに \+ / \- ボタンで数量を選択（在庫数以上の選択は不可、SOLD OUT品は選択不可）。  
  * 合計金額のリアルタイムプレビューと「カートに追加」ボタン。  
* **カート・会計パネル**:  
  * 選択中商品とトッピング内訳（例: └ \+ ホイップ × 2 (¥100)）を表示。  
  * 小計・合計金額の自動計算。  
  * **預かり金額入力**: クイック現金額ボタン（「ちょうど」「千円」「五千円」「一万円」）および手入力。  
  * お釣り金額の自動計算（不足時は警告表示）。  
  * 「会計確定・注文送信」ボタン押下で、在庫を減算し、連番の注文番号（例: \#1）を発行して注文を登録。

### **2.3 キッチン管理モジュール (Kitchen Display System: KDS)**

* **注文チケット（カンバン風表示）**:  
  * ステータスが WAITING（未調理）, PREPARING（調理中）, READY（受け渡し可）の注文を時系列でカード表示。  
  * 注文番号（大文字）、経過時間（「○分前」）を表示。  
  * **トッピング明細の強調表示**: メイン商品の配下に、選択されたトッピングと数量（例: \+ ホイップ × 2）を目立つバッジ形式で表示。  
* **ステータス進行機能**:  
  * 調理開始 (WAITING → PREPARING)  
  * 出来上がり (呼出) (PREPARING → READY)  
  * お渡し完了 (READY → COMPLETED)  
* **誤操作防止機能**: 「1つ前に戻す（Undo）」ボタンを配置。  
* **未処理件数バッジ**: タブ上に現在処理中の件数をリアルタイム表示。

### **2.4 呼び出し掲示板モジュール (Callout Display)**

* **大画面モニター用表示**:  
  * **お渡し可能エリア (READY)**: 大きな文字と目立つアニメーションで注文番号を表示。  
  * **調理中エリア (PREPARING / WAITING)**: 控えめな文字で待機中の注文番号を表示。

### **2.5 在庫 & 商品カタログ管理モジュール**

* **メイン商品一覧テーブル**:  
  * 商品名、基本価格、在庫数、SOLD OUTトグルスイッチ、手動在庫追加ボタン（+10, \+50等）。  
* **トッピング商品一覧テーブル (全共通)**:  
  * トッピング名、追加価格（/個）、在庫数、SOLD OUTトグルスイッチ、手動在庫追加ボタン。  
  * **新規トッピング追加機能**: 名称、単価、初期在庫数を指定してマスター追加可能。

### **2.6 売上ダッシュボードモジュール**

* **リアルタイムサマリーカード**:  
  * 総売上金額、総注文件数、メイン商品総販売数、トッピング総販売数。  
* **ランキング分析**:  
  * メイン商品別 売上ランキング（件数・構成比 %）。  
  * トッピング別 注文数ランキング（個数・構成比 %）。  
* **注文履歴ログ (直近50件)**:  
  * 注文番号、日時、購入内容（商品＋トッピング内訳）、合計額、ステータス。  
  * 注文取り消し機能（取消時にメイン商品および各トッピングの在庫を自動返却）。  
* **CSVデータエクスポート**:  
  * 全注文履歴データ（トッピング内訳含む）をCSVファイルとしてダウンロード。

## **3\. データベース（DB）設計仕様**

トッピングの個別の事前紐づけ（中間テーブル ProductTopping）を廃止し、独立したメイン商品・トッピング構成に変更したデータベース設計です。

\[Product (メイン商品)\] ───\< \[OrderItem (注文明細)\] \>─── \[Order (注文)\]  
                                    │  
\[Topping (トッピング)\] ───\< \[OrderItemTopping (明細トッピング)\]

### **3.1 テーブル定義**

#### **1\. Product (メイン商品)**

| カラム名 | 型 | 説明 |
| :---- | :---- | :---- |
| id | VARCHAR (PK) | 商品ID (例: p1) |
| name | VARCHAR | 商品名 (例: バニラソフトアイス) |
| price | INTEGER | 基本価格 (例: 300\) |
| stock | INTEGER | 現在の在庫数 |
| is\_sold\_out | BOOLEAN | 手動売り切れフラグ |

#### **2\. Topping (トッピングマスター \- 全共通)**

| カラム名 | 型 | 説明 |
| :---- | :---- | :---- |
| id | VARCHAR (PK) | トッピングID (例: t1) |
| name | VARCHAR | トッピング名 (例: ホイップクリーム) |
| price | INTEGER | 1個あたりの追加価格 (例: 50\) |
| stock | INTEGER | 現在の在庫数 |
| is\_sold\_out | BOOLEAN | 手動売り切れフラグ |

#### **3\. Order (注文ヘッダー)**

| カラム名 | 型 | 説明 |
| :---- | :---- | :---- |
| order\_id | VARCHAR (PK) | 注文一意ID |
| order\_number | INTEGER | 連番表示用注文番号 (1, 2, 3...) |
| status | VARCHAR | ステータス (WAITING, PREPARING, READY, COMPLETED, CANCELLED) |
| total\_amount | INTEGER | 決済合計金額 |
| deposit\_amount | INTEGER | 預かり金額 |
| change\_amount | INTEGER | お釣り金額 |
| created\_at | TIMESTAMP | 注文作成日時 |

#### **4\. OrderItem (注文明細 \- メイン商品)**

| カラム名 | 型 | 説明 |
| :---- | :---- | :---- |
| cart\_item\_id | VARCHAR (PK) | カート明細ID |
| order\_id | VARCHAR (FK) | 注文ID |
| product\_id | VARCHAR (FK) | メイン商品ID |
| product\_name | VARCHAR | 購入時の商品名 |
| unit\_price | INTEGER | 購入時の本体単価 |
| total\_price | INTEGER | 本体＋トッピングの明細合計額 |

#### **5\. OrderItemTopping (注文明細トッピング実績)**

| カラム名 | 型 | 説明 |
| :---- | :---- | :---- |
| id | VARCHAR (PK) | 履歴ID |
| cart\_item\_id | VARCHAR (FK) | 注文明細ID |
| topping\_id | VARCHAR (FK) | トッピングID |
| topping\_name | VARCHAR | 購入時のトッピング名 |
| price | INTEGER | 購入時の単価 |
| quantity | INTEGER | 選択された個数 |

## **4\. プログラミング・開発ガイドライン (OpenCode用指示)**

### **4.1 技術スタック推奨**

* **フロントエンド**: HTML5, Tailwind CSS (CDN/CLI), Vanilla JavaScript または React / Vue.js / Alpine.js。  
* **アイコン**: Font Awesome 6  
* **状態管理 / 通信**:  
  * クライアント完結の場合: BroadcastChannel API \+ LocalStorage  
  * バックエンド連携の場合: Node.js (Express/Fastify) / Go / Python \+ PostgreSQL / Supabase \+ WebSockets (Socket.io)

### **4.2 実装時の注意点**

1. **在庫引き当てのトランザクション**:  
   * 注文確定時、メイン商品の在庫減算と同時に**選択されたトッピングの個数分（quantity）の在庫も減算**すること。  
   * 在庫不足の場合はエラーダイアログを表示し、注文確定をブロックすること。  
2. **注文取り消し時の復元**:  
   * 注文取消時、メイン商品だけでなく**選択されていたトッピングの数量分も在庫数を復元**すること。  
3. **トッピングモーダルのUI/UX**:  
   * トッピングごとに \+ / \- で個数を増減できるカウンターUIを配置し、0個の場合はカートに含めない。  
   * 在庫が0または is\_sold\_out: true のトッピングは操作不可（disabled）にすること。